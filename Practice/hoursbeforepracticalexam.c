#include <stdio.h>
#include <string.h>

int main()
{
    FILE *ifp, *ofp;

    ifp = fopen("input.txt", "r");
    ofp = fopen("output.txt", "w");

    fprintf(ofp, "ORIGINAL     REVERSED\n");
    fprintf(ofp, "-----------------------\n");

    for



    return 0;
}